<h1>Welcome to the <%= @activity.name %> Page!</h1>
<h3>Difficulty Level: <%= @activity.difficulty %></h3>
<h3>Current Campers Enrolled:</h3>
<ul>
    <% @activity.campers.each do |camper| %>
        <li>
            <%= link_to camper.name, camper_path(camper) %>
        </li>
    <% end %>
</ul>

